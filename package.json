{
  "name": "scanar",
  "version": "1.0.0",
  "description": "Turn any QR code into an instant AR info panel in the browser.",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
